# Copy to .env.local for local dev. NEVER commit real keys (.env.local is gitignored).
# Used server-side only by /api/analyze and scripts/retag-corpus.mjs. Never exposed to the browser.

GEMINI_API_KEY=

# Optional: override the model (defaults to gemini-3.5-flash)
# GEMINI_MODEL=gemini-3.5-flash
